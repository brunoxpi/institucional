{
  "name": "anova-institucional",
  "version": "1.0.0",
  "description": "Site institucional da Anova — Sistema operacional conversacional para distribuição financeira",
  "private": true,
  "scripts": {
    "start": "npx serve public",
    "build": "echo 'Static site — no build step'"
  }
}
